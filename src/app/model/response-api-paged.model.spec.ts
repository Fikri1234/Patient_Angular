import { ResponseApiPaged } from './response-api-paged.model';

describe('ResponseApiPaged', () => {
  it('should create an instance', () => {
    expect(new ResponseApiPaged()).toBeTruthy();
  });
});
